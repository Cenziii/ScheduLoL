test.txtbody: Stack(
  children: [
    // Lista scrollabile dietro
    Padding(
      padding: const EdgeInsets.only(top: 70), // spazio per il carosello
      child: _isLoadingSchedule
          ? Center(child: CircularProgressIndicator())
          : ListView.builder(
              itemCount: _matches.length,
              itemBuilder: (context, index) {
                return CardMatch(match: _matches[index]);
              },
            ),
    ),

    // Carousel in sovrapposizione in alto
    Positioned(
      top: 0,
      left: 0,
      right: 0,
      child: Container(
        height: 70,
        padding: const EdgeInsets.symmetric(vertical: 8),
        decoration: BoxDecoration(
          color: theme.colorScheme.background.withOpacity(0.9),
          boxShadow: [
            BoxShadow(
              color: Colors.black.withOpacity(0.1),
              blurRadius: 4,
              offset: Offset(0, 2),
            ),
          ],
        ),
        child: CarouselView(
          controller: _carouselController,
          itemExtent: 80,
          shrinkExtent: 60,
          elevation: 0,
          children: _leagues.map((l) {
            final String? imgUrl = l.imageUrl;
            final String? leagueName = l.name;

            return GestureDetector(
              onTap: () async {
                setState(() => _currentLeague = l);
                await currentLeagueSchedule(_currentLeague.id);
              },
              child: Container(
                padding: const EdgeInsets.all(8),
                child: Material(
                  shape: const CircleBorder(),
                  elevation: 2,
                  clipBehavior: Clip.antiAlias,
                  color: theme.colorScheme.primaryContainer,
                  child: Ink.image(
                    image: NetworkImage(imgUrl ?? ''),
                    width: 50,
                    height: 50,
                    fit: BoxFit.cover,
                    child: InkWell(onTap: () {}),
                  ),
                ),
              ),
            );
          }).toList(),
        ),
      ),
    ),
  ],
),
